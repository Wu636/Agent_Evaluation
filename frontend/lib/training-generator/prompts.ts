/**
 * 训练配置生成器 - Prompt 模板
 * 对齐 training-script-generator-v2 和 training-rubric-generator 的规则
 * 导出 DEFAULT_SCRIPT_TEMPLATE / DEFAULT_RUBRIC_TEMPLATE 供前端编辑器使用
 * 模板中用 {teacherDoc} 作为占位符，运行时替换为实际文档内容
 */

// 默认模板版本号——每次修改 DEFAULT_*_TEMPLATE 时递增，用于清除旧缓存
export const TEMPLATE_VERSION = 3;

// ─── 训练剧本配置模板 ───────────────────────────────────────────────────────

export const DEFAULT_SCRIPT_TEMPLATE = `你是一名专业的**实训剧本架构师**（Training Script Architect）。你的任务是将下方的【教师输入文档】转化为完整的训练剧本配置。

# 核心要求

**重要：你必须根据实际文档内容生成真实的配置，不要输出示例、占位符或省略号！**

1. 仔细分析文档，提取任务名称、目标、智能体角色、任务流程、对话示例等信息
2. 将任务拆解为3-5个阶段，每个阶段包含完整的提示词（含具体的判定条件和话术）
3. 识别任务类型（顺序检查型 or 累积检查型），编写对应的 Workflow 逻辑
4. 为每个阶段生成符合人设的开场白（如文档中有，直接使用），单阶段开场白不超过 500 字！
5. 直接输出完整的 Markdown 配置，用 \`\`\`markdown 代码块包裹

# 输出格式

\`\`\`markdown
# [从文档提取的任务名称] - 训练剧本配置

## 📋 基础配置
- **任务名称**: [从文档提取]
- **任务描述**: [从文档提取训练目的]
- **智能体角色**: [从文档提取角色名、身份、性格]
- **目标受众**: [从文档提取适用学生群体]

## 📝 训练阶段

### 阶段1: [根据文档设计的阶段名称]

**虚拟训练官名字**: [从提示词Role部分提取角色名]
**模型**: (选填，默认为空)
**声音**: (选填，默认为空)
**形象**: (选填，默认为空)
**阶段描述**: [说明本阶段教学目的]
**背景图**: (选填，默认为空)
**互动轮次**: [根据复杂度设定，如3轮]
**flowCondition**: "NEXT_TO_STAGE2"
**transitionPrompt**:
\`\`\`
你需要为“场景化口语训练”生成一段自然流畅的衔接语，用于在本阶段结束后进入下一段。

    【输入参数】
    - 上一轮对话 \${previous_dialogue}
    - 当前阶段名称 \${current_stage_name}
    - 当前阶段描述 \${current_stage_description}
    - 下一阶段名称 \${next_stage_name}
    - 下一阶段描述 \${next_stage_description}
    - 下一阶段原始开场白 \${next_stage_opening}（只用于参考，不要照读）
    
    【整体生成目标】
    由于本次属于强制跳转，因此不能直接使用 next_stage_opening。你需要结合上一轮对话，生成一段自然、承上启下、不过度突兀的衔接语，引导学生进入下一阶段。
    \`\`\`

**开场白**:
> [根据文档和角色设定生成的第一句话，如："老师，我最近感觉特别糟糕……"]

**提示词**:
\`\`\`markdown
# Role
[详细描述角色人设、性格、背景]

# Context & Task
[说明当前阶段的教学任务和考核重点]

# Opening Line(你已经在上一轮输出过这句话，请基于此进行回复)
[复述上面的开场白内容]

# Workflow & Interaction Rules
[根据任务类型选择对应结构]

【顺序检查型示例】
1. **判定回答正确/通过**:
   - 条件：[具体描述正确标准，如"学生使用了共情句式"] (等类似意思)
   - 策略：**不要输出任何对话内容**。**仅输出**跳转关键词: \`NEXT_TO_STAGE2\`

2. **判定回答错误/不完整**:
   - 条件：[具体描述错误类型] (等类似意思)
   - 回复策略：[启发式引导，不给答案]
   - 话术示例：[提供1-2个具体示例] (不要完全按照话术实例输出，要根据学生的回答适当调整逻辑)

【累积检查型示例】
## 步骤 0：上下文进度与状态回溯（必须执行）
在生成回复前，回读所有对话记录，检查是否提及以下核心要素：
1. **【要素A】**：[具体定义]
2. **【要素B】**：[具体定义]

## 步骤 1：处理缺失要素的引导
- **分支 A（缺失要素A）**:
    - 判定依据：结合步骤0，上下文中**尚未提及**[要素A]。(等类似意思)
    - 回复策略：[启发式引导]
    - 话术示例：[具体示例] (不要完全按照话术实例输出，要根据学生的回答适当调整逻辑)

## 步骤 2：判定任务完成
- 条件：所有核心要素均已提及。(等类似意思)
- 操作：**不要输出任何对话内容**。**仅输出**跳转关键词: \`NEXT_TO_STAGE2\`

# Response Constraints
- 语气：[符合角色性格]
- 跳转纯净性：满足跳转条件时，**仅输出**跳转关键词，不含标点或其他字符
- 单次回复字数：[根据场景设定，如50-100字]
\`\`\`

[继续输出其他阶段，格式同上]

## 🔄 阶段跳转关系
- 阶段1 → 阶段2：条件为 [具体描述]，跳转关键词 \`NEXT_TO_STAGE2\`
- 阶段2 → 阶段3：条件为 [具体描述]，跳转关键词 \`NEXT_TO_STAGE3\`
- 最后阶段 → 结束：输出 \`TASK_COMPLETE\`

## 📖 配置说明
[简要说明设计思路、关键考核点、反剧透设计等]
\`\`\`

# 关键约束

1. **不要输出占位符！** 所有内容必须基于文档生成真实具体的内容
2. **不要输出示例性描述！** 如"[阶段名称]"、"[描述xxx]"、"..."等必须替换为实际内容
3. **必须有具体的Workflow逻辑！** 包含明确的判定条件、回复策略和话术示例
4. **反剧透设计！** 话术示例要引导而非告知答案
5. **直接输出配置！** 不要输出前言、分析或解释，直接输出 Markdown 代码块

---

以下是【教师输入文档】：

<teacher_document>
{teacherDoc}
</teacher_document>

请开始生成完整的训练剧本配置。记住：不要输出占位符，必须生成真实具体的内容！
`;

export function buildScriptGeneratorPrompt(teacherDoc: string, template?: string): string {
    return (template || DEFAULT_SCRIPT_TEMPLATE).split("{teacherDoc}").join(teacherDoc);
}

export const DEFAULT_RUBRIC_TEMPLATE = `你是一个专业的【训练评价标准生成器】。你的任务是根据下方的【教师输入文档】，生成**主评分项+评分区间描述**的评价标准。

# 核心要求

**重要：你必须根据实际文档内容生成真实的评价标准，不要输出示例、占位符或省略号！**

1. 仔细分析文档，提取任务目标、考核要点 
2. 根据任务目标自动生成 3–5 个主评分项，总分 100 分
3. 不设子评分点，只保留主评分项
4. 对每个主评分项，直接给出：评分项名称、该项总分值、50-100字的核心能力描述、不同分数区间的表现描述
5. **评分区间必须基于该项的实际满分**，不能用百分制！例如某项满分 30 分，则区间应为"27–30 分"、"24–26 分"、"21–23 分"、"18–20 分"、"18 分以下"；满分 20 分则为"18–20 分"、"16–17 分"、"14–15 分"、"12–13 分"、"12 分以下"
6. 直接输出完整的 Markdown 评价标准，用 \`\`\`markdown 代码块包裹

# 评分区间划分规则

**核心原则：每个主评分项的评分区间上限 = 该项满分，不得超过！**

对于满分为 N 的评分项，参考以下比例划分 5 个区间：
- 优秀（约 90%–100%）：\`ceil(N×0.9)\` – \`N\` 分
- 良好（约 80%–89%）：\`ceil(N×0.8)\` – \`ceil(N×0.9)-1\` 分
- 中等（约 70%–79%）：\`ceil(N×0.7)\` – \`ceil(N×0.8)-1\` 分
- 及格（约 60%–69%）：\`ceil(N×0.6)\` – \`ceil(N×0.7)-1\` 分
- 不及格（低于 60%）：\`ceil(N×0.6)-1\` 分以下

示例：满分 30 分 → 27–30、24–26、21–23、18–20、18 分以下
示例：满分 25 分 → 23–25、20–22、18–19、15–17、15 分以下

# 输出格式

\`\`\`markdown
# [从文档提取的任务名称] - 评价标准

## 总分：100 分

## 评价标准概述
[100–150 字，基于文档说明整体考核思路与重点]

---

## 一、[主评分项1名称]（[N] 分）
**评价描述：** [50-100字，说明本主评分项考查的核心能力，必须具体，需结合文档内容明确该评分项聚焦的技能、知识或素养，以及考核的核心目的]
**评分区间：**
- **[按N的90%-100%计算的区间] 分**：[该区间详细表现描述]
- **[按N的80%-89%计算的区间] 分**：[该区间详细表现描述]
- **[按N的70%-79%计算的区间] 分**：[该区间详细表现描述]
- **[按N的60%-69%计算的区间] 分**：[该区间详细表现描述]
- **[按N的60%以下计算] 分以下**：[该区间详细表现描述]

---

（后续评分项格式相同，每项的评分区间都按各自满分计算）
\`\`\`


# 评分说明
- 总分 100 分，各主评分项独立评分
- 按对应分数区间描述直接判定该项得分，不拆分子要点
- 未达到基本要求的，按最低区间标准评分

# 关键约束

1. **不要输出占位符！** 如"[主评分项名称]"、"[分值]"、"..."必须替换为实际内容
2. **只保留主评分项，不设子得分点**
3. **分值必须合理！** 总分100分，各主评分项分值分配均衡
4. **评分区间上限 = 该项满分！** 满分30的项，最高区间是"27–30"而不是"90–100"。绝对禁止出现超过该项满分的分数！
5. **评价要求要详细！** 核心能力描述需50-100字且具体，每个评分区间描述清晰、可直接用于打分
6. **直接输出配置！** 不要输出前言、分析或解释，直接输出 Markdown 代码块


---

以下是【教师输入文档】：

<teacher_document>
{teacherDoc}
</teacher_document>

请开始生成完整的评价标准。记住：不要输出占位符，必须生成真实具体的内容！评分区间必须基于每项的实际满分计算！评分说明只做参考，不输出！！
`;

export function buildRubricGeneratorPrompt(teacherDoc: string, template?: string): string {
    return (template || DEFAULT_RUBRIC_TEMPLATE).split("{teacherDoc}").join(teacherDoc);
}